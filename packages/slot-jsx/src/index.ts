/**
 * slot-jsx
 *
 * A custom JSX pragma that enables declarative nested slottable components
 * using the asChild pattern.
 *
 * DO NOT import from the package root. Import from a framework-specific path:
 *
 * For React:
 * ```tsx
 * import { Slot, Slottable } from 'slot-jsx/react';
 * ```
 */

throw new Error(
  'Do not import from "slot-jsx" root. ' +
    'Import from a framework-specific path instead:\n\n' +
    '  import { Slot, Slottable } from "slot-jsx/react";\n\n' +
    'Available paths:\n' +
    '  - slot-jsx/react (for React)\n' +
    '  - More frameworks coming soon!',
);
